#ifndef TYPES
#define TYPES

#include <deque>
#include <vector>

// Forward declaration
class Agent;

typedef double Real;
typedef std::deque<Agent> Container;
typedef std::vector<Agent*> TemporaryContainer;

#endif
